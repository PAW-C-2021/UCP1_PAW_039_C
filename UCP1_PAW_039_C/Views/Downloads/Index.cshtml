@model IEnumerable<UCP1_PAW_039_C.Models.Download>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Materi)
            </th>
            <th>
                @Html.DisplayName("Url Materi")
            </th>
            <th>
                @Html.DisplayName("Educational Stage")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Materi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UrlMateri)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EducationalStage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDownload"><i class="fa fa-marker fa-lg"></i></a>
                <a asp-action="Details" asp-route-id="@item.IdDownload"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdDownload"><i class=" fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
